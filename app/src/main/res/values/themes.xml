<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme. -->
    <style name="Theme.DBS_Prank" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@color/white</item>
        <item name="colorControlNormal">@android:color/transparent</item>
    </style>

    <!-- Theme for SplashActivity. -->
    <style name="Theme.DBS_Prank.Splash" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@color/white</item>
    </style>
</resources>
